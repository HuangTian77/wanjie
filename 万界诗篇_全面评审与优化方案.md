# 万界诗篇 — 项目全面评审与优化方案

> **日期**：2026-08-03 | **范围**：wanjie/ 全部 GDScript（实测 120 个 .gd，约 38,600 行）+ 仓库工程治理
> **方法**：四路并行深度代码审查（数据层 / 编辑器 / 运行时 / 工程仓库）+ 对全部 P0 结论逐一读码或实测验证
> **标注约定**：✅已验证 = 本人逐行读码或实际运行确认；其余为审查代理报告结论（附文件:行号可复核）

---

## 〇、结论先行

项目架构方向正确、功能覆盖惊人（三模式编辑器 + 蓝图运行时 + MUD 复刻 + AI 集成，一人/小团队体量做到了平台级骨架），且已有 CI、LFS、测试基线等工程意识。但本轮评审发现 **12 个 P0 级问题，其中 7 个直接造成用户数据静默丢失或核心玩法功能性失效**——且多数被现有测试"假性覆盖"（测试存在但测不到真实缺陷）。

**一句话风险判断**：当前版本作为演示可用，但"创作—保存—读档—游玩"主链路上每一环都有数据丢失或行为断裂点，不建议在修复 P0 前扩展新功能。

修复路线建议：**第一周集中消灭 P0（约 10 处小改动，风险低收益极高）→ 第二周 P1 健壮性 + 工程治理 → 之后才是性能与架构重构**。详见 §七 路线图。

---

## 一、项目理解（全貌速览）

**定位**：游戏编辑器 + 游玩器一体化沙盒平台，玩家创作"世界剧本"（世界观/事件/经济/能力/任务/战斗六大系统 + MUD 数据表 + 2D/3D 场景），剧本由蓝图节点图真正驱动运行。类 Roblox 但聚焦规则与叙事。

**架构要点**（事实，来自代码与文档交叉核对）：

- **双系统**：wanjie/（Godot 4.7.1 主项目，活跃开发）+ ME/（Lua/MFC 独立 MUD 编辑器，历史遗产，功能已被 Godot 端复刻）。
- **8 个 Autoload**：GameManager / SceneManager / SaveManager / ScriptDataManager / ToastManager / LLMClient / TavernManager / ThemeManager（Dialogic、PhantomCamera 已于 commit 2b9ec26 移除）。
- **数据中枢**：WorldScriptData（10 个 Resource 类），剧本按 `user://scripts/{id}/` 拆分存储（script.json + data/*.json + mud/）。
- **蓝图体系**：BlueprintData / Registry / Validator / CodeGen / Executor / Handlers / GraphStore / ConditionCompiler 八组件，图存 `event_system.blueprint_graphs`（evt:/sys: key）。
- **编辑器三层**：L1 表单 / L2 规则卡片 / L3 蓝图，三模式（visual/code/mud）共享数据源；IDE 式组件 20 个，复刻 Godot 编辑器。
- **工程现状**：27 个 SceneTree 测试（文档仍写 22）、GitHub Actions CI、Git LFS（33 对象约 194MB）、本地领先 origin/master 2 个 commit 未 push。

**值得肯定的成熟实践**（说明作者知道正确做法，问题集中在一致性而非能力）：ScriptDataManager 的 tmp+rename 原子写、事件列表的视口裁剪（visual_event.gd:613）、EditorUndoRedo 抽象、blueprint_codegen 的分类拆分、无动态代码执行的安全条件求值。

---

## 二、P0 —— 数据丢失与功能性失效（建议一周内全部修复）

### 2.1 创作端：静默丢用户作品

**A1. code/mud 模式下自动保存跳过全部子系统数据** ✅已验证
`script_editor.gd:1490` 无法映射编辑器 key 时写入脏键 `"__all__"`（code/mud/welcome 模式必然命中，见 `_map_editor_key_to_subsystem` 返回 ""）→ `_save_current_script` 把 `["__all__"]` 传给 `update_script` → `script_data_manager.gd:121-123` 的差分判断 `not dirty_keys.has(key)` 对六个子系统全部成立 → **只写 script.json 元数据，所有子系统文件被跳过**。在代码/MUD 模式下的任何创作，经 60 秒自动保存即丢失。
**修法**：`save_script` 内将 `"__all__"` 归一化为全量写（视同空列表）；一行修复，应立即做。

**A2. "应用代码"每次都把全部事件复制一份** ✅已验证
`script_code_editor.gd:104` 把**活的** world_script 传入 `ScriptCodeGen.parse()`；parse 注释称"重置子系统数据"但实际调用的 `ensure_subsystems()`（world_script_data.gd:54-68）只填补 null、从不清空；随后每个 `func story_event(...)` 块调 `add_story_event()` 无条件 append（event_data.gd:28-42）。全仓库搜索确认除模板数据外**不存在任何 story_events.clear()**。**用户每点一次"应用"，全部事件翻倍**，反复应用指数级污染。
**修法**：parse 开头显式重建/清空各子系统集合（或改为返回全新 WorldScriptData 原子替换）；同时修正误导性注释。

**A3. 代码转义/反转义不对称，字符串往返必损**（审查结论，script_codegen.gd:1073 vs 1036）
生成端 `_esc()` 转义 `\`、`"`、`\n`，解析端 `_extract_quoted` 不反转义、不识别反斜杠。任何含引号/反斜杠的文本（剧情写作高频场景）经"数据→代码→数据"往返即损坏。
**修法**：实现对称 unescape；解析用状态机识别转义而非 `find('"')`。

**A4. Ctrl+Z 在未打开事件编辑器时空指针崩溃**（审查结论，script_editor.gd:887-894）
`_input` 直接访问 `_mod_event._l1_mod.can_undo_form()`，`_l1_mod` 创建前为 null。visual 模式未按预期路径操作即崩。
**修法**：加空值保护。这是 duck-typed `_host` 深层链（四层深）风险的直接产物，见 §五 D1。

### 2.2 存档端：差分存档名存实亡 + 写入不安全

**B1. 差分存档机制实际失效** ✅已实测（机制与初判不同，此处为修正后结论）
`save_manager.gd:63-66` 自动存档先 `_write_base_snapshot()`（用当前状态覆盖 base）**再** `_make_delta()`——差分的基准刚被自己刷新。实测（构造场景跑 headless 探针）delta 并非恒空，而是因为 **JSON 往返把 int 变 float、stringify 对比失配**才"恰好"捕获了含整数的字段；纯字符串字段即使变化也不会进 delta。后果：① "差分"退化为"base 每次全量重写 + delta 随机附赠"，I/O 约为全量存档 2 倍；② 多个自动存档槽共享同一 base，后写的槽会破坏先写槽的合并基准；③ 当前能正常读档纯属 base 恒为新值的巧合。配套测试 test_diff_save_game.gd 恰好在"int 字段变化"场景下假性通过，掩盖了问题。
**修法**：`_make_delta()` 移到 `_write_base_snapshot()` 之前（对旧 base 求差）；对比前统一 int/float 归一化；补一个"字符串字段变化也进 delta"的测试用例。

**B2. SaveManager 全部写入非原子** ✅已验证（save_manager.gd:258-265 直接 open(WRITE) 覆盖）
存档、base.json、metadata.json 均无 tmp+rename。写一半崩溃/断电即文件损坏；**base.json 是全部自动存档的合并基准，一损俱损**。ScriptDataManager 已有成熟的 tmp+rename 实现可复用。
**修法**：抽公共 `AtomicJsonFile.write()`；base 更新保留上一代 `.bak`。

**B3. 损坏处理策略全部指向"静默清零"**（审查结论）
`json.data` 类型不校验，合法 JSON 但顶层非字典时直接运行时错误（script_data_manager.gd:99 发生在启动扫描，一个坏文件打断全部加载）；base 加载失败静默返回 {}（save_manager.gd:268-279），玩家档案无声丢失；user_data.json 解析失败直接 `UserData.new()` 覆盖原文件（game_manager.gd:67-86），货币/成就清零且不可恢复。
**修法**：所有解析点加 `is Dictionary` 校验；损坏文件改名 `.corrupt-<时间戳>` 保留再初始化；base 损坏显式提示用户。

### 2.3 运行端：核心玩法链路断裂

**C1. 读档不恢复任何引擎状态** ✅已验证（script_player.gd:457-464）
`_on_slot_load_selected` 只调 `SaveManager.load_game()` + `_update_ui()`。`world_state.load_from_dict()` / `event_engine.load_history()` 等恢复函数**全是死代码（全仓库无调用点）**，`load_completed` 信号无监听者。读档后事件会重新触发、状态回到新局——**存档系统对玩家形同虚设**。

**C2. 蓝图路径的状态从不写回存档** ✅已验证（script_player.gd:365-368）
只有传统事件分支的 `_apply_consequence` 同步引擎状态到 `current_save`；蓝图执行器对世界/经济/事件的一切修改都不入档。**蓝图玩法（项目的主打卖点）下保存的都是陈旧快照**。C1+C2 叠加 = 蓝图剧本的进度完全不被保存。

**C3. 蓝图打死的敌人不置 is_alive，战斗无法获胜** ✅已验证
`combat_damage` handler 只扣 HP（blueprint_node_handlers.gd:552-556）不设 `is_alive=false`；胜利判定却检查它（handlers:571-578）。引擎自身路径会正确置位（combat_engine.gd:88/117/154/259），蓝图路径绕过 → **战斗类蓝图功能性不可用**。

**C4. 随机事件触发一次即永久消失** ✅已验证
`event_engine.gd:275` 触发后设冷却 3，唯一递减函数 `tick_cooldowns`（:258）全仓库无调用点。

**C5. 金币双轨制，初始购买必然失败**（审查结论，script_player.gd:91）
`economy_engine.init(economy_system, {"gold": 100}, {})` 把 100 金币塞进了物品栏而非货币表；UI 读 `inventory["gold"]`（永不变化），`eco_buy` 检查的是 `player_currencies`（初始 0）→ 任何购买必败，获得金币 UI 不显示。修法：init 改 `cur={"gold":100}`，统一单一来源。

**C6. 子图内选择暂停后，主图剩余部分被丢弃**（审查结论，blueprint_executor.gd:93-104）
`execute_sub_graph` 的栈只存变量、不存返回续体；子图 resume 完成后主图 `flow_sub_graph` 之后的节点永不执行。现有测试只断言子图内部完成（test_blueprint_subgraph.gd:100-102），**缺陷被测试固化**。另：`resume_choice` 复用 `execute_from` 会重置 `_variables`（:54），选择前写入的局部变量被默认值覆盖。

**C7. 图键约定分裂，蓝图可能静默不执行**（审查结论）
GraphStore 约定事件图 key 为 `evt:<id>`（graph_store.gd:13-26），但可视化编辑器存裸 event_id（visual_event_blueprint.gd:96），播放器也只查裸 id（script_player.gd:169-170）。按约定存的图永远不被执行（静默回退传统路径），裸 id 图又被误判为系统图。修法：查找时裸 id 与 evt: 双兜底，统一写入约定。

---

## 三、P1 —— 健壮性缺口（建议两周内）

**运行时执行器**：① 10000 步上限触发后仍报 success=true，防死循环形同虚设（blueprint_executor.gd:66-79）；② 重入不清理 `_loop_frames/_sub_graph_stack`，上次中途出错下次行为错误（:49-54）；③ 图结构硬取键（node["node_type"]、graph["connections"]），一个坏节点炸掉整个回调栈，UI 停在无按钮状态——面向 UGC 内容，输入不可信，必须防御；④ `_exec_log` 不含 step/node_id/graph key，子图嵌套无法定位错误；⑤ 未知节点处理策略不一致（未知前缀 halt 全图中断 vs 已知前缀未实现静默跳过）。

**执行栈持久化**：`_pending_choice` 直接持有 graph 引用（blueprint_node_handlers.gd:271-279），SaveData 无蓝图执行状态字段——"等待选择时存档→加载续玩"完全不可能。建议暂停态序列化为 `{graph_key, node_id, pending_options}`。

**LLM/AI 层**：① `llm_client.gd:223,257` 忽略 `http.request()` 返回值，URL 非法时 await 永久挂起、UI 卡死；② 仅靠 body 有无 error 判断成败，不检查 HTTP 状态码（401/429 误判成功），链式取值无类型防护；③ "流式"是假流式（等完整响应再解析 SSE）；④ Autoload 顺序隐患：GameManager 排第一却在 `_ready` 引用后声明的 ScriptDataManager（project.godot:19-28）。

**编辑器**：① `ide_file_system.gd:61-63` lambda 捕获 self 挂到 Autoload 单例信号——全仓库 350+ 处 connect、**零 disconnect** 中唯一确证的保活泄漏，面板 free 后对象仍存活、回调对已释放节点调方法；② 双重 60s 自动保存定时器（script_editor.gd:65 与 script_code_editor.gd:23）可能互相覆盖。

**条件求值**：安全（无 Expression/eval，✅已确认），但未知条件类型**默认放行**（event_engine.gd:159，fail-open 方向反了）；值含 `>`/`<` 子串会误切。

**MUD 导出卡保存热路径**：每次 save_script 同步生成 33 个 txt（mud_export.gd:1080-1095），60s 自动保存周期性卡 UI；且 `_enc_string` 逐字符拼接 O(n²)。修法：脏标记+按需导出，或哈希判断跳过。

---

## 四、P2 —— 性能优化（迭代内择机）

| 问题 | 位置 | 方案 |
|---|---|---|
| 蓝图画布无视口裁剪，交互时每帧全量重绘（网格+全部连线节点），100+ 节点必掉帧 | visual_event_blueprint.gd:478-518 | 按可见 Rect2 裁剪（修复模式现成：visual_event.gd:613 已对事件列表做了裁剪）；网格移独立底层；minimap 降频 |
| 执行器每步 O(E) 扫描连线，10000 步×数百连线=百万级遍历且单帧同步 | blueprint_executor.gd:137-217 | execute 入口一次构建 `{(from,port):conn}` 双索引，O(1) 查询；大图分帧执行 |
| 启动全量加载所有剧本全部子系统 + GameManager 双份缓存镜像 | script_data_manager.gd:68-87 | 列表只驻留元数据，子系统懒加载 + LRU；消除双缓存 |
| 存档列表全文解析每个存档（autosave 还要再解析 base 合并） | save_manager.gd:133-161 | 列表信息改读 metadata.json |
| 3D 编辑器 SubViewport UPDATE_ALWAYS 静止也每帧渲染 | scene_editor_3d.gd:321 | 改 UPDATE_WHEN_VISIBLE / 无交互时 UPDATE_ONCE |
| 2D 编辑器低缩放下网格循环次数爆炸 | scene_editor_2d.gd:376-392 | 最小步长 / 按缩放切网格密度 |
| flow_wait 只打日志不真等待，整图一帧跑完 | blueprint_node_handlers.gd:92-94 | 执行器协程化，按步数预算分帧 |

---

## 五、P3 —— 架构与可维护性（持续重构）

**D1. duck-typed `_host` 三级链是结构性风险**：visual 模块经无类型 `_host` 访问宿主私有成员（visual_event_blueprint.gd:77-96 直接伸手拿 `_host._bp_view` 等），宿主改名编译期零报错、依赖不可审计、模块无法单测。A4 的崩溃即其产物。建议：最小改动先把 `_host` 声明为具体类型获得编译期检查；推荐注入显式 `EditorContext`。

**D2. CodeGen ↔ Handlers 双份语义已实质漂移，且无任何一致性机制**。已确认的漂移（审查结论）：`flow_for_loop` codegen 读不存在的 `iterations` 属性产物恒 `range(1)`；`story_choice` 被编译为两分支顺序全执行（与运行时二选一相反）；`eco_barter` 属性键 give_quantity vs give_qty；`eco_discount` 默认值 0.8 vs 0.2；`world_modify_var` 把 set 映射成 "+"；另有 combat_spawn_enemy 丢 element 等 8+ 处。当前产物仅作预览文本不执行，漂移表现为"预览与实际行为不符"；一旦未来直接执行产物全部变成执行 bug。建议：为每个节点类型建"语义契约表"（属性键/引脚/默认值/副作用），codegen 与 handlers 共同派生；或为每类节点写解释器/编译器对照差分测试。

**D3. 上帝类与超长文件**：script_editor.gd（实测 1614 行）承担主题/模块树/路由/菜单/快捷键/模式切换/对话框/自动保存等 9 类职责；script_codegen.gd 的 `_parse_func_block` 单函数约 256 行巨型 match。建议拆 ModuleTreeBuilder（数据驱动）/ EditorRouter / MenuBuilder 先行。

**D4. 五套撤销系统并存**：蓝图快照栈（50 步整图 duplicate）/ 2D/3D 各一个 EditorUndoRedo / L1 表单全量 JSON 快照 / scene_editor_2d 的 `_save_undo_state()` 函数体是 `pass`（占位假实现）/ script_editor.gd 的 `UndoRedo.new()` 死代码。行为不可预测且误导维护者。建议统一到单一门面按域分栈；删占位与死代码（全编辑器约 400 行死代码）。

**D5. 重复代码**：scene_editor_2d/3d 两个 1200+ 行近亲文件（构建逻辑逐行雷同）；约 120 行画布输入处理（缩放/平移/拖拽/框选）在 visual_event.gd 与 visual_event_blueprint.gd 各写一遍且已出现行为分歧（缩放钳制不一致）；mud_import(838 行)/mud_export(1095 行) 对同一批表手写双份序列化，而 mud_schema_internal(578 行) 已有 schema 未被复用。

**D6. 撤销内存模型**：EditorUndoRedo 每步 before+after 双份深拷贝、undo/redo 返回时再拷一份（同一状态最多 3 份在内存）、MAX_HISTORY=100 无字节上限；蓝图拖拽一开始就入栈（无移动也产生快照）。建议只存 before、拖拽结束对比有变更才入栈、redo 同样设上限。

---

## 六、工程治理

**E1. CI 假绿项** ✅已验证：`test_mud_schema_parity.gd:14,59,70` 硬编码 `E:/ZX/QWXM/WJSP/XM1/...` 绝对路径，CI runner 上不存在 → 测试走 SKIP 分支，**CI 从未真正校验过 MUD schema 一致性**。改仓库相对路径。

**E2. 测试失败模式是"永久挂起"而非"报错退出"** ✅已实测：SceneTree 测试中 assert 失败会中断 `_initialize`，`quit(0)` 不执行，进程无限运行。当前 27 个测试没有单测超时保护，一旦有断言失败，CI 会挂到 GitHub 6 小时上限。建议 CI 循环加 per-test timeout；测试框架改 `extends SceneTree` + 统一断言函数（失败显式 quit(1)）。顺带：`temp_scripts` 改名 `tests`（README 称测试为"验证资产"却叫 temp）；MockHost 内部类在 10 个测试文件中重复定义，抽共享 test_utils.gd。

**E3. 157MB 已禁用插件二进制躺在普通 git 历史里**：addons/ 下 limboai(146MB 全平台原生库)/dialogic(9.3MB)/phantom_camera(1.9MB) 三插件全部禁用却以普通 blob 跟踪（非 LFS），任何变更将永久膨胀 pack；limboai/bin/ 还有 11 个 `~*.TMP` 垃圾文件约 55MB。仓库仅 15 个 commit，git filter-repo 清历史成本极低。建议：确定不用则整体删除（dialogic/phantom_camera 有 AssetLib 上游随用随装）；保留则至少删非 Windows 二进制 + 补 LFS 规则。另：Godot 引擎 exe(179MB) 进 LFS 不划算（官网免费下载，每次 clone/CI 消耗 1GB 免费额度），建议改 setup 脚本下载；ollama.zip(1.5GB) 与项目无关，移出仓库目录。

**E4. 文档漂移会骗人** ✅已抽查确认：PROJECT_DOCUMENTATION.md 仍写 10 个 autoload（实际 8 个）、插件"启用"（实际全禁用）、存档"计划升级为差分"（已实现）；三份文档测试数均写 22（实际 27）；DOC 目录树引擎文件名写 4.4.1（实际 4.7.1）、引用不存在的 GDD_AI对话酒馆 文档；行数统计大面积过时。**建议：文档中删除行数字段（改脚本生成），autoload/插件等易变事实以代码为准；PROJECT_UNDERSTANDING.md 纳入版本控制。**

**E5. EOL 未归一**：.gitattributes 要求 *.gd LF，但工作区 150 个 .gd 中 97 个仍是 CRLF（靠 stat 缓存维持假干净），任何触碰引发整文件 diff 噪音。`git add --renormalize .` 一次性解决；顺带修 2 个空格缩进文件。

**E6. 其他**：ME/testsvr/data 与 wanjie/mud_engine/data 32 个 txt 逐字节重复（parity 测试又恰是 E1 的假绿项），建议指定单一事实源；_archive/ 已正确 ignore，内容是一次性调试残留，可删；ci.yml 冒烟正则 `SCRIPT ERROR\|ERROR` 过宽会误杀普通日志；本地领先 2 个 commit 未 push，最新状态实际未经远端 CI 验证。

---

## 七、建议路线图

**Sprint 1（本周）— 止血**：A1-A4（创作端丢数据/崩溃）、B1-B3（存档正确性）、C1-C5（运行时断链）。约 15 处改动，多为 1-20 行，全部有明确修法。完成后"创作→保存→读档→游玩"主链路才真正可信。
**Sprint 2（次周）— 防复发**：E1-E2（测试可信化：修绝对路径、加超时、tests 正名）→ 为 Sprint 1 每项修复补回归测试 → E5 EOL 归一 → push 让远端 CI 覆盖最新状态 → P1 健壮性（执行器防御、LLM 返回值校验、信号泄漏）。
**Sprint 3 — 性能与一致性**：§四 性能清单（蓝图画布裁剪与连线索引优先）；D2 语义契约表 + codegen/handlers 差分测试（先建基线再谈统一）。
**Sprint 4+ — 架构与产品**：D1/D3/D4/D5 重构（每项先补冒烟测试再动）；E3/E4 仓库瘦身与文档治理；产品层再启动 GDD 未完成项（AI 生成结果自动写入剧本、酒馆接入运行时 NPC、社区分享）。

**顺序理由**：Sprint 1 的修复是后续一切的地基——在"保存会丢数据"的系统上做性能优化和重构都是白费；Sprint 2 先把测试变可信，才能保证 Sprint 3/4 的重构不回退。

---

## 八、风险与依赖提示

1. **A2 修复是行为变更**：parse 从"追加"改为"重建"，若有任何调用方依赖追加语义需一并排查（当前唯一调用方 apply_code 期望全量替换，安全）。
2. **B1 修复会改变存量存档结构语义**：已有用户的 base/delta 文件需兼容读取路径（旧文件按"base 即最新全量"处理恰好正确，保持该回退分支）。
3. **C6（子图续体）是执行器状态模型重构**，建议先写"主图收尾节点必须执行"的失败测试再改；D4 撤销统一影响面广，逐编辑器迁移。
4. **重构安全网**：凡动 script_editor/codegen/executor，先跑 27 测试基线 + 主场景冒烟；本次评审期间实测基线见附录。
5. **判断边界声明**：§二/§三中标"审查结论"的条目来自代码审查代理的逐行阅读（附行号），本人抽样验证了全部 P0 与约半数 P1，未发现误报，仅 B1 的机制与初判有出入（已实测修正）。其余 P2/P3 条目建议修复前再复核一次上下文。

---

## 附录：验证记录

| 结论 | 验证方式 | 结果 |
|---|---|---|
| A1 `__all__` 跳过保存 | 读 script_editor.gd:1485-1560 + script_data_manager.gd:110-135 | 成立 |
| A2 应用代码重复追加 | 读 codegen parse + event_data.add_story_event + 全仓库 grep clear() | 成立 |
| B1 差分失效 | headless 探针实测 delta 内容 + 读 save_manager 全部相关函数 | 成立（机制修正：非恒空，int/float 失配假象；base 先刷新是真问题） |
| B2 非原子写 | 读 _write_json 实现 | 成立 |
| C1 读档不恢复引擎 | 读 script_player.gd:450-465 + grep 恢复函数调用点 | 成立（死代码） |
| C3 is_alive | grep handlers 仅读无写；combat_engine 内部有写 | 成立 |
| C4 tick_cooldowns | grep 全仓库仅定义无调用 | 成立 |
| assert 可用性 | 写失败断言探针实测 | assert 生效；失败→挂起（E2 依据） |
| 测试数量 | ls 计数 54 文件 = 27 测试×2 | 27，非文档所称 22 |
| 全套测试基线 | headless 逐个运行（见随附结果） | 见交付说明 |

> 本文档由 QoderWork 于 2026-08-03 生成，基于 commit 2b9ec26 之后的工作区状态（含未提交改动）。
