# 万界诗篇 · 可视化编辑器深度研究与设计方案

> **文档类型**：深度研究报告 + 产品设计方案
> **核心命题**：让完全不会编程、没有游戏开发经验的普通用户，也能通过可视化编辑器做出逻辑清晰、系统完整、功能复杂的游戏/剧本作品
> **产品定位**：游戏编辑器 + 游玩器（类罗布乐思的"创作 + 体验"双轮平台），而非单纯的文字剧本工具
> **撰写依据**：基于项目现有代码（`wanjie/scripts/editor/`、`resources/data/`、`scripts/player/`）、《GDD_万界诗篇_游戏开发方案.md》及业界竞品调研
> **版本**：v1.0

---

## 0. 执行摘要（一页读懂）

**一个核心判断**：项目已经拥有了相当完整的"专业向"编辑能力（蓝图节点图、代码编辑器、MUD 表编辑、四大子系统表单面板、Godot 风格 IDE 骨架），但这些能力对零基础用户而言门槛过高。**当前缺的不是"更多功能"，而是一个"低门槛入口层"和一条"渐进成长路径"。**

**一个核心方案**：**三层渐进编辑模型（L1 表单层 → L2 规则层 → L3 图表层）**，三层共享同一份数据（多视图单源），用户从"填空式创作"起步，随能力成长无缝升级到"规则卡片"，最终进入"节点图/代码"，全程不返工、不重写。

**三个最高杠杆的改造点**：
1. **结构化条件模型**：把事件条件从字符串表达式（`"faction_001.power_level > 70"`）重构为结构化对象（主体+字段+运算符+值），这是"表单能编辑逻辑"的前提，可双向编译兼容现有 `event_engine`。
2. **When-Do 规则卡片**：用"当……时 → 执行……"这一种心智模型统一事件/经济/AI/场景交互，替代让小白望而生畏的自由节点图。
3. **模板即游戏 + 随时试玩**：每个模板都是一个完整可玩的成品，创作流程从" remix 一个成品"开始；编辑全程 F7 一键试玩 + 时间旅行调试，形成"编辑—运行—修正"闭环。

**一句话设计理念**：**"像填表格一样开始，像搭积木一样进阶，像写蓝图一样精通，而作品始终是一个完整可玩的游戏。"**

---

## 1. 产品定位：不是剧本工具，是游戏创作与游玩平台

### 1.1 从"剧本编辑器"到"游戏创作平台"的再定位

当前项目代码中大量命名仍停留在"剧本（script）"语境（`script_editor.gd`、`script_player.gd`、`ScriptDataManager`），GDD §1.1 则早已定义了更大的愿景："玩家既是造物主（创造世界剧本），也是旅者（体验世界剧本）"。本方案明确：**"剧本"只是载体，"游戏"才是产品**。

| 维度 | "剧本工具"思维（现状残留） | "游戏创作平台"思维（目标） |
|------|--------------------------|--------------------------|
| 用户创作的是什么 | 一段可阅读的文字故事 | 一个可运行、可交互、有规则的游戏世界 |
| 编辑器的核心 | 文字录入 + 数据表填写 | 规则设计 + 逻辑编排 + 实时验证 |
| 成功标准 | 故事写完了 | 游戏好玩、能跑通、别人能玩 |
| 复杂度来源 | 文本量 | 系统间的规则涌现（GDD §1.2 "规则涌现"） |
| 游玩器地位 | 附属的"预览器" | 与编辑器平起平坐的核心半壁 |

**关键结论**：编辑器的一切设计，都要回答一个问题——"用户这样配置后，**运行起来**会怎样？"。任何不能落到"可运行"的编辑功能都是无效功能。这正是 GDD §1.2 "剧本即游戏"卖点的落地要求。

### 1.2 双轮驱动：造物主模式 × 旅者模式

GDD §2.1 定义了"创造循环"与"体验循环"双循环。可视化编辑器必须让这两个循环**高频互穿**，而不是割裂的两个场景：

```
        ┌────────────── 造物主模式（编辑器）──────────────┐
        │  构思 → 配置规则 → 测试运行 → 调优 → 发布        │
        └──────┬───────────────────────────────▲──────────┘
               │ 一键试玩(F7)                    │ 返回编辑
               ▼                               │
        ┌────────────── 旅者模式（游玩器）──────────────┐
        │  进入世界 → 探索 → 做选择 → 承受后果 → 反馈      │
        └──────────────────────────────────────────────┘
```

**设计约束**：从"编辑"到"试玩"的往返成本必须低于 3 秒（保存+启动），否则用户会放弃频繁验证，退回到"写完一大坨再碰运气跑一次"的高风险模式。现有 `ide_workspace.gd` 的"游戏"视图 + `run_requested` 信号 + F7 快捷键已具备骨架，需要补的是**热重载**与**调试态游玩**（见 §9）。

### 1.3 "低门槛上手 × 高上限扩展"：Scratch 的三面墙理论

MIT Scratch 的设计哲学是 **Low Floor（低地板）、High Ceiling（高天花板）、Wide Walls（宽墙壁）**，这是本项目最应对标的理念：

- **低地板**：5 分钟内产出第一个"能跑、有反馈"的作品（不是"能保存的空壳"）。
- **高天花板**：能力上限不受工具限制——高级用户能用节点图/代码表达任意复杂逻辑（项目已有 `blueprint_data.gd` 10 类节点 + `script_codegen.gd` 代码生成，天花板已存在）。
- **宽墙壁**：支持多种创作路径与风格——叙事向、经营向、战斗向、解谜向的用户都能找到适合自己的模块与模板，而不是只有一条"标准做法"。

**现状诊断**：项目当前是"**高天花板、窄墙壁、高地板**"——专业能力强，但入门台阶太高（打开编辑器先看到模块树+三模式切换+代码区），且创作路径单一（都从"填世界观文本"开始）。本方案的核心就是把地板降下来、把墙壁拓宽。

### 1.4 竞品对标与差异化

| 产品 | 创作范式 | 低门槛手段 | 高上限手段 | 本项目可借鉴 | 本项目差异点 |
|------|---------|-----------|-----------|-------------|-------------|
| **Roblox Studio** | 拖拽场景 + Lua 脚本 | 模板库、模块化组件、6步新手引导 | Lua 全能力 | 模板 remix、组件库 | 我们聚焦**规则与叙事**而非 3D 空间建造 |
| **UE Blueprint** | 节点图可视化脚本 | 类型着色、引脚校验、搜索 | 图灵完备 | 节点类型配色、连线校验（项目 `blueprint_data.gd` 已实现 `validate_connection`） | 节点图只作 L3，不作入门层 |
| **原神·千星奇域** | 纯可视化节点蓝图（零代码） | 拖拽连线、实时预览、联机测试 | 节点图自定义战斗/AI | 零代码定位、实时预览 | 其痛点正是我们的机会（见下） |
| **Scratch** | 积木块拼接 | 积木只能以合法方式拼合 | 事件/并发/变量俱全 | "非法操作不可能发生"的防错思想 | 我们从表单起步而非积木 |
| **GDevelop/Construct** | 事件表（条件→动作） | When-Do 行式逻辑，无需空间布局 | 扩展行为、表达式 | **When-Do 规则卡片（本方案 L2 核心）** | 我们的规则绑定四大子系统 |
| **Twine** | 叙事节点图（卡片连线） | 极简"写文字+加链接" | 变量/条件宏 | 事件链可视化（项目 `event_chains` 已有 nodes/links） | 我们有完整游戏系统不止叙事 |
| **RPG Maker** | 事件命令列表 + 数据库表单 | 表单化数据库、命令式事件 | 脚本插件 | 表单化属性面板（项目 `panels/` 已有雏形） | 我们支持系统级自定义 |

**千星奇域的教训（来自玩家实测，直接可借鉴）**：
- ❌ "断开节点连线需右键点击删除" → 操作不直观 ⇒ 本方案要求连线交互支持"拖开即断"。
- ❌ "搜索节点时中英文切换规则不统一" → 检索混乱 ⇒ 节点/模块检索必须中文优先+拼音/英文兼容。
- ❌ "debug 模式信息不直观，大多数情况只能打印出一堆数字" → 调试失败 ⇒ 本方案把"可视化调试"列为一等公民（§9）。
- ❌ "元件缺乏继承和多态机制" → 高阶受限 ⇒ 模板/模块需支持"派生与覆写"。

---

## 2. 目标用户画像与用户路径

### 2.1 三类核心用户画像

| 画像 | 小白·叙事者（占比最高，核心目标） | 进阶·系统设计师 | 专业·世界架构师 |
|------|--------------------------|------------------|------------------|
| **典型身份** | 学生、网文爱好者、TRPG 跑团玩家 | 数值策划爱好者、MOD 作者 | 独立开发者、资深 GM |
| **编程能力** | 零 | 懂一点逻辑/Excel 公式 | 会写代码 |
| **核心诉求** | "把我脑子里的故事变成能玩的游戏" | "我要设计一套自洽的经济/战斗规则" | "我要完全掌控每个细节" |
| **主要使用层** | L1 表单层 + 模板 | L2 规则层 | L3 图表层/代码 |
| **最怕什么** | 看到代码、报错看不懂、不知从哪下手 | 规则冲突查不出来、数值失衡 | 工具能力封顶、被向导束缚 |
| **成功标志** | 1 小时内做出第一个能玩的作品并分享 | 做出一个经济自洽的中型世界 | 做出社区精品、被大量体验 |

**设计原则**：默认界面只服务"小白·叙事者"。进阶与专业能力通过"解锁/切换"获得，绝不默认暴露。

### 2.2 小白用户的完整创作流程（黄金路径）

这是产品最重要的流程，必须做到**每一步都有明确下一步指引、每一步都可见成果**：

```
① 新建项目
   └─ 不面对空白，而是面对"模板画廊"
      （奇幻冒险/科幻探索/历史策略 + 官方示例成品）
② 选择模板 → 立即进入一个"已经能玩"的世界
   └─ 先让用户 F7 玩 2 分钟模板成品，建立"我的游戏"实感
③ 可视化配置（L1 表单层，填空式）
   ├─ 世界：改名字/背景/势力（表单+下拉，不写表达式）
   ├─ 角色：捏一个主角（属性滑块+预设职业）
   ├─ 事件：用"事件卡片"写剧情（发生了什么→玩家能选什么→各自后果）
   └─ 每改一处，右侧"世界速览"实时刷新
④ 实时预览
   └─ F7 随时试玩；试玩中可开"导演视角"看事件触发情况
⑤ 调试修正
   └─ 试完回到编辑器，问题清单直接列出"第3个事件无法触发：缺少前置"
      点击问题 → 自动跳到对应编辑位置 → 一键修复建议
⑥ 发布/游玩
   └─ 导出/分享给朋友；或继续用 L2 规则层加深度
```

**关键设计点**：
- **②的"先玩再改"是留存关键**。小白对"创作"的恐惧来自"不知道结果长什么样"。模板成品先玩后改，把"创作"转化为"修改一个看得见的东西"，心理门槛骤降。
- **③绝不出现"请先学习"的环节**。所有引导以"下一步建议"卡片形式内嵌在流程中（如"你的世界还没有主角，点这里创建一个"）。
- **⑤的"问题→定位→修复"闭环**是区别于"只会报错不会指路"的工具的核心体验。

### 2.3 用户旅程地图与情绪设计

| 阶段 | 用户动作 | 目标情绪 | 设计手段 | 失败风险（需防） |
|------|---------|---------|---------|----------------|
| 进入 | 打开编辑器 | 好奇、安全 | 模板画廊+成品试玩，无空白页 | 面对空编辑器不知所措 |
| 首次修改 | 改一个名字/加一个事件 | "真的变了！" | 修改即时反映到速览/试玩 | 改了看不到效果 |
| 首次遇阻 | 事件不触发 | 不慌 | 问题清单+人话解释+一键定位 | 报错天书、不知所以 |
| 首次完成 | 跑通一个完整小故事 | 成就感 | 通关贺彩+引导分享 | 卡在最后一步 |
| 想更深入 | 想做"会涨价的市场" | 掌控感 | L2 规则卡片自然承接 | 发现要学代码而放弃 |

---

## 3. 核心设计理念：三层渐进编辑模型

### 3.1 为什么"节点图"不是小白的答案（最重要的认知纠偏）

项目现有 `script_editor.gd` 的"可视化模式"实际是**蓝图节点图编辑器**（`blueprint_data.gd` 定义了 start/branch/sequence/get_var/set_var/story_event/random_event/print/expression/comment 共 10 类节点、6 种引脚类型）。这是优秀的 L3 工具，但**把它当作"可视化入门"是一个普遍的行业误区**：

- 节点图要求用户同时理解**空间布局 + 执行流 + 数据类型 + 连线规则**四件事，认知负荷极高。
- 小白真正的心智模型不是"连线"，而是"**填空**"（写故事）和"**如果……就……**"（定规则）。
- 佐证：Scratch 用积木而非节点；GDevelop/Construct 用事件表而非节点；Twine 用卡片链接而非自由图；原神千星奇域用纯节点图，结果玩家反馈"零基础也能上手"主要靠的是模板而非节点本身，且 debug 体验被诟病。

**结论：节点图保留为高级层（L3），入门层必须是表单（L1），中间用规则卡片（L2）过渡。**

### 3.2 三层模型定义

```
┌─────────────────────────────────────────────────────────────┐
│  L3 图表层 / 代码层（高天花板）                                  │
│  蓝图节点图(blueprint_data.gd) + 代码编辑器(script_code_editor)  │
│  自由度最高，图灵完备；面向专业用户                               │
├─────────────────────────────────────────────────────────────┤
│  L2 规则层（进阶）                                              │
│  "当 X 发生时 → 执行 Y" 规则卡片；条件/动作下拉化               │
│  统一事件/经济/AI/场景交互的逻辑编辑；面向进阶用户               │
├─────────────────────────────────────────────────────────────┤
│  L1 表单层（低地板）★ 新增，本方案重点                            │
│  填空式表单 + 向导 + 模板；写故事、捏角色、配世界                 │
│  零门槛，非法输入不可能发生；面向小白用户                         │
└─────────────────────────────────────────────────────────────┘
              ▲ 三层共享同一数据模型（多视图单源）▲
        WorldScriptData + 四子系统 + blueprint_graphs
```

| 层 | 编辑形式 | 能表达什么 | 不能表达什么 | 面向 |
|----|---------|-----------|-------------|------|
| **L1 表单** | 输入框/下拉/滑块/卡片 | 世界观、角色、物品、标准剧情事件 | 复杂条件组合、自定义逻辑 | 小白 |
| **L2 规则** | When-Do 卡片（条件行+动作行） | 条件触发、数值联动、AI 行为、经济规则 | 循环、复杂数据流 | 进阶 |
| **L3 图表** | 节点图 / GDScript 风格代码 | 一切 | — | 专业 |

### 3.3 多视图单源：同一份数据，三种面貌（架构基石）

**绝对原则：三层不是三个编辑器，而是同一份数据的三个"投影/渲染器"。**

- 用户在 L1 表单里给事件填了一个触发条件"玩家等级 ≥ 5"；
- 切到 L2，它显示为一张条件卡片 `[玩家] [等级] [≥] [5]`；
- 切到 L3，它显示为一个条件节点或一行代码 `player.level >= 5`；
- 无论在哪层修改，写回的都是**同一个结构化条件对象**，运行时引擎读到的永远是同一份数据。

**为什么必须如此**：
1. **防止数据分裂**：如果三层各存各的，用户升级层级时旧数据丢失，等于逼用户重做——这是大多数"渐进式编辑器"失败的根本原因。
2. **支持自由升降级**：用户可以在 L1 写故事、L2 调规则、L3 改细节，随时切换，互不破坏。
3. **运行时零感知**：`event_engine`/`economy_engine` 不需要知道数据是哪层编辑的，只消费统一的运行时格式。

**实现要点**：引入一个**数据投影层（Projection Layer）**，负责"结构化模型 ⇄ 各层视图模型"的双向转换。L1/L2 是"受控视图"（只能表达子集），L3 是"完全视图"。当 L3 编辑出了 L1/L2 无法表达的结构时，该对象在低层显示为"高级内容（只读卡片）"，点进去提示"在图表层编辑"，**绝不静默丢弃**。

### 3.4 层级间的升级触发器（让用户自然成长）

不让用户"主动决定要学进阶"，而是让**需求驱动升级**：

- 用户在 L1 想加一个条件，但表单只提供"等级/金币/位置"等常用项 → 点"更多条件…" → 自然滑入 L2 条件卡片。
- 用户在 L2 想表达"循环 3 次"或"复杂计算" → 卡片表达不了 → 提示"这个逻辑可以用节点图表达，为你转换？" → 一键将该规则升级为 L3 节点子图。
- 每次升级都**只升级当前这一个对象**，其余保持在用户舒适的层级。**永远不整体强制升级。**

---

## 4. 可视化编辑方式选型矩阵

### 4.1 各编辑形式适用场景分析

| 编辑形式 | 认知负荷 | 适合表达 | 不适合 | 本项目落位 |
|---------|---------|---------|--------|-----------|
| **表单化属性面板** | ★ 最低 | 静态属性：角色数值、物品、势力、世界规则 | 逻辑、流程 | L1 主力；检查器（`ide_inspector.gd`） |
| **卡片/列表（Twine式）** | ★★ 低 | 剧情事件、对话、选择分支 | 复杂条件组合 | L1 事件编辑 |
| **条件-动作行（GDevelop式）** | ★★ 低 | 触发规则、AI 行为、经济联动 | 数据流计算 | **L2 主力（When-Do 卡片）** |
| **流程图/状态机** | ★★★ 中 | 有清晰状态转移的逻辑（NPC 作息、战斗阶段） | 涌现型规则 | L2 的可视化展示视图 |
| **节点图（蓝图式）** | ★★★★ 高 | 任意复杂逻辑、数据流 | 快速录入大量内容 | L3（现有 `blueprint_data.gd`） |
| **行为树** | ★★★★ 高 | 复杂 AI 决策 | 叙事 | L3 扩展（LimboAI 已预留，GDD §6.6） |
| **拖拽式场景编辑** | ★★ 低 | 空间布局、地图 | 抽象规则 | 2D 场景编辑器（`scene_editor_2d.gd`，Phase 3） |
| **事件触发器（Trigger）** | ★★ 低 | "进入区域→播放剧情" | 复杂数据流 | L2 场景交互模块 |

### 4.2 本项目选型结论

**"表单 + 卡片 + When-Do + 节点图"四件套**，按层级分配：

- **L1 = 表单 + 卡片**：世界观/角色/物品用表单，剧情事件用"事件卡片"（一张卡 = 事件描述 + 若干选择 + 每个选择的后果，全部下拉/填空）。
- **L2 = When-Do 规则卡片 + 流程图只读视图**：所有"触发型逻辑"统一为规则卡片；事件链以流程图形式可视化（复用 `event_chains` 的 nodes/links 数据）。
- **L3 = 节点图 + 代码**：现有蓝图与代码编辑器，作为高天花板保留。
- **场景编辑**：2D 拖拽独立成工作区（`ide_workspace.gd` 的 2D 视图，Phase 3），不与逻辑编辑混在一起。

**一个关键的"不做"决策**：**不**在入门层引入自由连线的节点图，**不**要求小白理解"引脚/执行流/数据类型"。这些概念只在 L3 出现。

---

## 5. 复杂逻辑封装：模块 + 规则 + 条件 + 动作

### 5.1 设计理念：把"游戏系统"封装成"可组合的玩法模块"

小白不懂"事件系统/经济系统/能力系统"这些工程概念，他们想的是"**我要一个会涨价的市场**""**我要一个会巡逻的守卫**""**我要一个背叛的桥段**"。因此封装的单位应该是**玩法模块（Gameplay Module）**——一个自带"组件+规则+预设"的完整功能包：

| 玩法模块 | 提供的组件（可拖出/添加） | 内置规则（可调参） | 对应现有系统 |
|---------|------------------------|------------------|-------------|
| **剧情事件** | 事件卡片、选择、分支 | 触发条件、前置事件 | `event_system`（story_events/branches） |
| **随机遭遇** | 遭遇池、权重表 | 概率、冷却、难度缩放 | `event_system`（random_events/weight_table） |
| **市场经济** | 货币、商品、市场 | 供需定价、通胀、波动 | `economy_system` + `economy_engine` |
| **角色成长** | 技能、属性、成长路线 | 升级曲线、解锁条件 | `ability_system` |
| **战斗** | 敌人、技能、元素相克 | 伤害公式、回合制 | `combat_engine` |
| **对话** | NPC、对话、选项 | 好感度、信息解锁 | Dialogic 集成（GDD §6.6） |
| **任务** | 任务、目标、奖励 | 接取/完成条件 | 待建（GDD quest_log 已有数据结构） |
| **AI 行为** | NPC 行为（巡逻/守卫/商贩） | 触发反应、警戒度 | 待建（LimboAI 预留） |
| **场景交互** | 区域、触发器、传送点 | 进入/离开事件 | `scene_editor_2d.gd` + 事件触发器 |

**模块化的价值**：用户添加"市场经济"模块时，不需要理解供需公式，只需要拖出"市场"组件、设定"这个市场卖什么、贵不贵"，公式（`economy_engine` 已实现供需定价）在背后自动运转。这就是 GDD §1.2 "规则涌现"的落地——**用户配置"是什么"，引擎负责"会怎样"**。

### 5.2 When-Do 规则卡片：万能的逻辑原语

所有模块的"动态逻辑"统一为一种形式——**规则卡片（Rule Card）**：

```
┌─ 规则卡片 ────────────────────────────────┐
│ 规则名：[守卫发现小偷]                        │
│                                            │
│ 当（触发器）：  [玩家] [进入] [王都集市]        │
│ 且（条件）：    [玩家] [金币] [<] [0]         │
│ 且（条件）：    [守卫] [警戒度] [>] [50]      │
│                                            │
│ 就（动作）：    [弹出事件] → [被抓事件]        │
│ 且（动作）：    [玩家金币] [-] [50]           │
│ 且（动作）：    [守卫好感] [-] [10]           │
└────────────────────────────────────────────┘
   [+ 添加条件]   [+ 添加动作]   [⋯ 转为节点图]
```

**为什么 When-Do 是小白逻辑的最佳形式**：
- 它是**语言化的**："当……且……就……"读起来就是一句话，不需要空间思维。
- 它是**受控的**：条件和动作都是下拉选出来的，**不可能写出语法错误**。
- 它是**强大的**：GDevelop/Construct 证明，纯 When-Do 事件表足以做出商业级独立游戏。
- 它**可组合**：多张规则卡片叠加就是复杂系统（守卫+市场+经济=会抓小偷的活跃城市）。

### 5.3 结构化条件模型（最高杠杆的数据改造）★

**现状问题**：`event_data.gd` 中条件是 `{"type": "world_state", "check": "faction_001.power_level > 70"}`——`check` 是**字符串表达式**。这意味着：
- 小白无法手写（不知道 `faction_001.power_level` 这种写法）；
- 编辑器无法用下拉框渲染它（不知道里面是什么）；
- 容易写出运行时才爆炸的错误表达式。

**改造方案**：引入**结构化条件（Structured Condition）**，作为条件的唯一存储格式，字符串表达式降级为"编译产物"：

```gdscript
## 结构化条件数据模型（新增 resources/data/condition_data.gd）
## 树形结构：叶子是原子条件，分支是逻辑组合
StructuredCondition := {
    "logic": "and",          # and / or / not（分支节点）
    "children": [],          # 子条件（分支节点用）
    # —— 叶子节点字段 ——
    "subject_type": "player",  # player/faction/world/time/item/npc/variable
    "subject_id": "",          # 具体对象ID（如 faction_001），player/world 可空
    "field": "level",          # 字段（level/gold/power_level/relationship...）
    "operator": ">=",          # > < >= <= == != contains in
    "value": 5,                # 比较值（类型由 field 的 schema 决定）
}
```

**双向编译器**（新增 `condition_compiler.gd`）：
- **编译（编辑→运行）**：`StructuredCondition → "player.level >= 5"`，交给现有 `event_engine` 的表达式求值器，**运行时零改动**。
- **反编译（运行→编辑）**：解析旧字符串表达式 → 结构化条件。能解析的转为结构化；解析不了的（用户手写的高级表达式）标记为 `"raw": "..."`，在 L1/L2 显示为只读"高级条件"卡片，L3 可编辑。**保证不丢数据。**

**这一个改造同时解锁**：L1 表单能选条件、L2 卡片能渲染条件、检查器能校验条件合法性（字段是否存在、类型是否匹配）、问题清单能定位"哪个条件引用了不存在的势力"。

### 5.4 动作（Action）的统一模型

与条件对称，动作也结构化：

```gdscript
StructuredAction := {
    "action_type": "modify_stat",  # modify_stat/give_item/trigger_event/
                                   # set_variable/play_dialog/change_relation/
                                   # spawn_npc/teleport/...
    "target_type": "player",       # player/faction/world/npc/market
    "target_id": "",
    "params": { "field": "gold", "op": "-", "value": 50 },
}
```

动作库按模块组织，每个动作都有：中文名称、图标、参数表单、**实时效果预览**（如"金币-50"在速览里立刻显示变化后的数值）。

### 5.5 模块联动与涌现（复杂玩法的来源）

复杂玩法不来自"写复杂逻辑"，而来自**模块间的规则交叉**。编辑器应主动提示联动可能：

- 用户建了"市场"模块 + "守卫"AI 模块 → 系统提示："要不要加一条规则：物价上涨时守卫警戒度提高？"（一键生成 When-Do 卡片）
- 用户在事件里给了玩家"星辰碎片" → 系统提示："检测到新物品，要不要让市场可以交易它？"

这就是 GDD "规则涌现"的产品化：**编辑器不只是让用户组合规则，还帮用户发现组合的可能。**

---

## 6. 防错与引导体系

### 6.1 防错的最高原则：让错误不可能发生（Poka-Yoke）

优先级排序：**预防 > 检测 > 提示 > 修复**。

| 层级 | 手段 | 例子 |
|------|------|------|
| **预防（不可能错）** | 受控输入：下拉、滑块、类型约束 | 条件字段只能从该主体的合法字段里选；数值用滑块带合理区间 |
| **检测（错了立刻知道）** | 实时校验，不等"保存/运行" | 输入事件名时即时查重；引用不存在的势力立即标红 |
| **提示（看懂哪里错）** | 人话错误 + 定位 + 建议 | "第3个事件无法触发：它的前置事件'星辰陨落'不存在。[点击定位] [创建该事件]" |
| **修复（一键解决）** | 自动修复建议 | "是否自动创建一个名为'星辰陨落'的空事件？[是]" |

### 6.2 模板库与示例项目（真正的新手引导）

**小白不读文档，他们 remix 成品。** 模板体系设计：

- **模板即完整游戏**：每个模板（奇幻冒险/科幻探索/历史策略，见 `get_templates()`）都必须**开箱即玩**——有主角、有事件、能通关。当前模板偏"数据骨架"，需升级为"可玩成品"。
- **示例项目带"教学注释"**：官方示例剧本里内嵌"这里是这么实现的"引导气泡，用户点事件卡片时可看到"这个事件用了：前置条件+2个选择"。
- **remix 优先于新建**：新建入口第一位永远是"从模板开始"，"空白新建"弱化为高级选项。
- **模板分层**：每个题材分支提供"极简版（5分钟通关）/标准版/进阶版"三档，匹配不同起点。

> 注：`get_templates()` 已按题材分支组织（奇幻冒险/科幻探索/历史策略），"龙焰纪元"等具体剧本名已从分支列表移除、归入奇幻冒险分支作为示例成品——这正是"分支=题材，剧本=成品"的正确模型。

### 6.3 新手向导（Wizard）

不做"开机教学弹窗"，做**任务式向导**：

- 首次进入：向导带用户完成"改一个名字 → 加一个事件 → F7 试玩"三步，5 分钟产出第一个作品。
- 向导以"待办清单"形式常驻侧栏（"① 给世界起个名字 ② 创建主角 ③ 写第一个事件 ④ 试玩一次"），完成一项勾一项，可随时关闭。
- 每完成一项给正反馈（进度条/徽章），利用 GDD §2.2 的短期目标设计。

### 6.4 上下文提示与参数校验

- **字段级提示**：每个表单项有"?"图标，悬停显示"这是什么+合理范围+例子"（如"通胀率：每年货币贬值比例，建议 0~5%"）。
- **区间护栏**：数值输入给软区间（超出显示黄色"不常见"而非直接禁止），既防错又不限制创意。
- **引用完整性**：所有"引用型"字段（前置事件、目标势力、物品）用选择器而非手输 ID，从源头杜绝"引用不存在"。

### 6.5 逻辑冲突检测（把 `script_validator.gd` 产品化）

项目已有 5 维度校验（`script_validator.gd`），需从"报错列表"升级为"诊断医生"。检测项：

| 检测 | 算法 | 用户语言 |
|------|------|---------|
| **死路径** | 事件图可达性分析（GDD §8.3.3） | "事件'屠龙'永远无法触发：它要求完成'获得圣剑'，但没有任何选择能获得圣剑" |
| **死循环** | 事件链环检测 | "事件 A→B→C→A 形成循环，会导致剧情卡死" |
| **条件矛盾** | 条件约束求解 | "这个事件要求等级>50，但你的世界最高等级是 30" |
| **资源不可达** | 物品产出/消耗图 | "任务需要'星辰碎片'，但世界里没有它的来源" |
| **经济失衡** | 蒙特卡洛模拟（GDD §8.1.3） | "模拟 1000 天后：金币严重通胀，物价涨了 8 倍。建议增加消耗点" |
| **选择失衡** | 分支收益对比 | "选项A收益远高于其他选项，玩家会总选A，分支失去意义" |

**呈现方式**：问题按严重度分级（错误/警告/建议），每条 = 人话描述 + [定位] + [一键修复（若可用）]。校验不是"发布前一次性"，而是**后台持续运行**，状态栏常驻"✅ 无问题 / ⚠️ 2 个警告"。

### 6.6 可视化报错与自动修复

- 错误直接**标注在出问题的组件上**（事件卡片红框+角标），而不是只进错误列表。
- 常见错误提供自动修复：缺失前置→建议创建；引用失效→建议替换为存在的对象；数值越界→建议拉回区间。
- 修复永远"建议+确认"，不静默改数据（呼应 GDD §8 AI 原则"草案/建议，确认后生效"）。

---

## 7. 与现有系统结合：数据安全与三方一致

### 7.1 现有数据模型盘点（改造的起点）

| 资产 | 现状 | 与可视化编辑器的关系 |
|------|------|---------------------|
| `WorldScriptData` | 容器：metadata + 四子系统引用 + `ensure_subsystems()` | ✅ 结构良好，作为单源数据根 |
| `WorldviewData` | 背景故事/世界规则/势力 | 直接适配 L1 表单 |
| `EventSystemData` | story_events/random_events/event_chains/condition_pool/blueprint_graphs（**Dictionary 数组，条件为字符串**） | ⚠️ 条件是字符串表达式，需结构化改造（§5.3） |
| `EconomySystemData`/`AbilitySystemData` | 货币/资源/市场/技能/成长 | 直接适配 L1 表单 + L2 规则 |
| `blueprint_graphs` | 每事件一个节点图（nodes/connections/local_variables） | ✅ L3 已就绪，需建立"规则卡片 ⇄ 子图"互转 |
| 拆分存储 | `script.json` + `data/{worldview,events,economy,abilities}.json` + `mud/` + `assets/` | ✅ 天然支持"按模块分文件"，视图层无感知 |
| 四引擎（`scripts/player/`） | world_state/event_engine/economy_engine/combat_engine | ✅ 运行时只读统一格式，编辑器不直接碰引擎 |

### 7.2 编辑器 / 运行时 / 游玩器 三方一致原则

**单一数据流向（铁律）**：

```
  L1表单 ─┐
  L2规则 ─┼──► 结构化数据模型（唯一真相源）──► 编译 ──► 运行时格式 ──► 四引擎
  L3图表 ─┘        （WorldScriptData+子系统）        （含条件表达式串）   （游玩器）
       ▲                                                    │
       └──────────── 试玩反馈/调试数据（只读回流，不改数据）──────┘
```

- **编辑器永不直接写运行时格式**：编辑器只写结构化模型，由 `condition_compiler` 等编译器生成运行时格式。避免"编辑器和引擎各维护一套格式"的分裂。
- **游玩器永不写回剧本定义**：试玩产生的状态（`world_state` 快照）属于存档域（`SaveManager`），与剧本定义域物理隔离（GDD §4.1 剧本独立性原则）。
- **MUD 数据表是导出产物**：`mud/` 下 32 个 txt 由 `MudExport` 从 `metadata["mud_data"]` 导出（`script_data_manager.gd` 已实现保存时自动导出）。可视化编辑器编辑的是 `mud_data` 源，**不直接改 txt**，导出由保存动作统一触发，保证一致。

### 7.3 安全读写策略

| 风险 | 防护 |
|------|------|
| 低层视图写坏高层数据 | 投影层只允许"增量修改自己渲染的字段"；L3 独有字段对 L1/L2 只读 |
| 旧字符串条件丢失 | 反编译失败的条件保留 `raw` 原文，标记 `"structured": false`，全程透传不改写 |
| 并发编辑冲突 | 单一活动视图原则：同一时刻只有一个层在编辑某对象，切层时先提交当前层 |
| 版本迁移 | 结构化条件带 `"cond_version"`；加载旧剧本走"一次性迁移"（项目已有 `_migrate_split()` 迁移范式，可复用） |
| 自动保存覆盖 | 复用现有 `_auto_save_timer`（60s）+ `_is_dirty` 机制；切层/试玩前强制落盘 |

### 7.4 拆分存储与视图层的映射

现有拆分存储（`data/` 四文件）与三层编辑是**正交**的：视图层操作内存中的 `WorldScriptData`，持久化由 `ScriptDataManager.save_script()` 统一负责拆分落盘。**视图层完全不感知文件结构**——这是上一阶段"拆分存储"改造带来的架构红利，本方案直接继承。

---

## 8. 编辑器架构设计

### 8.1 总体布局：Godot 风格 IDE 骨架（已具备，需重组）

项目已有完整的 Godot 风格 IDE 组件（`scripts/editor/ide/`），这是巨大的存量优势。目标布局：

```
┌──────────────────────────────────────────────────────────────────┐
│ 菜单栏(ide_menu_bar)  工具栏(运行▶ 调试🐞 校验✓ 层级[L1|L2|L3])      │
├────────┬───────────────────────────────────────────┬─────────────┤
│ 左Dock  │              中央工作区 (ide_workspace)       │  右Dock      │
│ ┌────┐ │  ┌─────────────────────────────────────┐  │ ┌─────────┐ │
│ │文件 │ │  │  2D │ 场景 │ 逻辑(L1/L2/L3) │ 游戏     │  │ │ 检查器   │ │
│ │系统 │ │  ├─────────────────────────────────────┤  │ │(inspector)│ │
│ │(fs) │ │  │                                     │  │ │ 属性表单  │ │
│ ├────┤ │  │   当前层的编辑画布                      │  │ │ 实时速览  │ │
│ │模块 │ │  │   L1: 表单+事件卡片                    │  │ ├─────────┤ │
│ │导航 │ │  │   L2: 规则卡片列表+流程图               │  │ │ 世界速览  │ │
│ │(tree)│ │  │   L3: 节点图/代码                    │  │ │ 问题清单  │ │
│ └────┘ │  └─────────────────────────────────────┘  │ └─────────┘ │
├────────┴───────────────────────────────────────────┴─────────────┤
│ 底部面板(ide_bottom_panel): 输出 │ 诊断(校验) │ 调试(事件追踪) │ 模板  │
├──────────────────────────────────────────────────────────────────┤
│ 状态栏(ide_status_bar): 层级指示 │ 校验状态 │ 光标 │ 自动保存状态     │
└──────────────────────────────────────────────────────────────────┘
```

### 8.2 模块划分与现有代码映射

| 架构模块 | 职责 | 现有代码 | 状态 |
|---------|------|---------|------|
| 项目文件系统 | 剧本项目文件夹浏览/文件编辑 | `ide_file_system.gd`（双模式） | ✅ 已有 |
| 模块/场景树 | 剧本结构导航 | `ide_scene_tree.gd` + `script_editor.gd._build_module_tree` | ✅ 已有 |
| 检查器 | 选中对象的属性表单 | `ide_inspector.gd` | ✅ 已有，需接 L1 表单 |
| 逻辑编辑区 | L1/L2/L3 三视图 | `script_editor.gd`(L3蓝图) + `script_code_editor.gd`(L3代码) + `panels/`(L1雏形) | ⚠️ L1/L2 待建 |
| 规则引擎投影 | 结构化条件/动作 ⇄ 表达式 | 无 | ❌ **新建 `condition_compiler.gd`** |
| 玩法模块库 | 模块注册/组件/预设 | `panels/`（四子系统面板） | ⚠️ 需模块化重组 |
| 输出/诊断面板 | 日志+校验+调试 | `ide_bottom_panel.gd` + `script_validator.gd` | ✅ 已有，需产品化 |
| 运行预览 | 一键试玩+调试态 | `ide_workspace.gd` 游戏视图 + `script_player.gd` | ⚠️ 需热重载+调试 |
| 模板中心 | 模板画廊/remix | `script_editor.gd` template_panel + `get_templates()` | ⚠️ 需升级为成品画廊 |
| 撤销重做 | 全局 undo/redo | `script_editor.gd._undo_redo` | ✅ 已有 |
| 自动保存 | 定时+脏标记 | `script_editor.gd._auto_save_timer` | ✅ 已有 |
| 新手向导 | 任务式引导 | 无 | ❌ 新建 |
| 世界速览 | 右侧实时反馈面板 | 无 | ❌ 新建 |

### 8.3 需要新建的核心模块（按优先级）

1. **`condition_compiler.gd`**（P0）：结构化条件/动作 ⇄ 表达式字符串双向编译。一切的地基。
2. **L1 表单层视图**（P0）：复用 `panels/` 四子系统面板 + 新"事件卡片"组件，挂在检查器/中央区。
3. **L2 规则卡片视图**（P1）：When-Do 卡片列表 + 规则 ⇄ 事件数据投影。
4. **世界速览面板**（P1）：右侧实时显示"当前配置下的世界长什么样"。
5. **诊断产品化**（P1）：`script_validator.gd` 输出 → 人话+定位+修复建议。
6. **调试游玩器**（P2）：`script_player.gd` 增加"导演视角"覆盖层 + 事件追踪。
7. **模板成品画廊**（P2）：`get_templates()` → 可试玩成品 + remix。
8. **新手向导**（P2）：任务式清单。

### 8.4 层级切换的 UI 设计

- 工具栏常驻层级切换器 `[L1 表单 | L2 规则 | L3 图表]`，默认 L1。
- 切换是**视图切换而非模式切换**——数据不变，只换渲染。
- 状态栏显示当前层级 + 一句话说明（"表单模式：适合快速搭建世界"）。
- 首次使用 L2/L3 时弹 30 秒微引导。
- **记忆用户偏好**：记住每个用户上次用的层级，但每个新对象仍从 L1 起步。

---

## 9. 可玩性验证：编辑—运行—修正闭环

### 9.1 闭环是创作体验的心脏

GDD §2.4 造物主心流："可视化编辑（即时反馈）→ 测试运行（验证成果）→ 调优迭代"。**反馈延迟越短，创作越像游戏；延迟越长，创作越像加班。** 目标：任意编辑后 ≤3 秒可试玩，试玩中随时可暂停回编辑。

### 9.2 一键运行与热预览

- **F7 全局试玩**（已有 `run_requested` 信号），从任何层、任何工作区可触发。
- **热重载**：试玩前自动保存 → 游玩器重新加载剧本定义 → 从指定起点开始。现有 `script_player.gd._init_engines()` 每次 `_ready` 重建引擎，已天然支持重载，需补"从指定事件/时间点开始"。
- **试玩起点选择**："从头开始 / 从当前编辑的事件开始 / 从上次中断处"——编辑事件时"从这个事件开始试"是最高频需求。

### 9.3 时间旅行调试（State Snapshot）

- 游玩器运行时，`world_state` 每个关键节点（事件触发前/选择后/每游戏日）自动打快照。
- 编辑器调试面板显示**时间轴**，用户可拖回任意快照"从那一刻重新玩"。
- 价值：用户发现"第 10 个事件不对"时，不必从头重玩，直接跳回第 9 个事件后。这是把"调试"变成"回放"的关键。

### 9.4 事件追踪与可视化报错（导演视角）

试玩时开启"导演视角"覆盖层，把 GDD 抱怨的"打印一堆数字"变成可视化：

- **事件触发浮标**：哪个事件被触发、因为哪些条件满足（绿色✓）/不满足（红色✗ + 原因）。
- **条件实时面板**：悬停任意事件卡片，显示它当前"为什么没触发"（"条件2不满足：玩家等级 3 < 5"）。
- **经济仪表盘**：试玩中实时显示物价/通胀曲线。
- **选择路径图**：把玩家本次游玩走过的事件链画成流程图（复用 event_chains 渲染），未走的分支灰色显示。

### 9.5 快速模拟与 AI 试玩（高上限验证）

- **蒙特卡洛快跑**（GDD §8.1.3/§8.3.1）："模拟 100 个玩家各玩 30 天"，10 秒出报告（事件覆盖率/经济曲线/死亡率/卡死点）。不需要 LLM，纯算法，本地可跑。
- **AI 试玩员**（GDD §8.3，Phase 后期）：LLM Agent 模拟不同类型玩家（探索型/速通型/极端型）走查剧本，产出"死路径/失衡/无聊点"报告。
- 这两者把"可玩性验证"从"作者自己玩"扩展到"统计意义上的验证"，是复杂作品质量的保障。

---

## 10. 数据模型建议

### 10.1 新增：结构化条件/动作（核心）

```gdscript
# resources/data/condition_data.gd（新建）
class_name StructuredCondition extends RefCounted
# 见 §5.3：树形 {logic, children[]} + 叶子 {subject_type, subject_id, field, operator, value}
# 附加字段: "cond_version": 1, "structured": true/false, "raw": ""（反编译失败时保留原文）

class_name StructuredAction extends RefCounted
# 见 §5.4：{action_type, target_type, target_id, params{}}
```

### 10.2 扩展：事件数据（渐进，不破坏）

```gdscript
# EventSystemData.story_events 中每个 event 增加可选字段（向后兼容）：
event := {
    # ...现有字段全部保留（id/name/description/trigger_type/conditions/choices/branches）...
    "conditions_structured": [],   # 新增：StructuredCondition 数组（与 conditions 字符串互为镜像）
    "choices": [{
        "id": "", "text": "",
        "consequences": [],          # 现有：字符串效果
        "actions_structured": [],    # 新增：StructuredAction 数组
    }],
    "edit_layer": 1,               # 新增：该事件最后在哪层编辑（用于打开时定位层级）
}
```

**兼容策略**：`conditions`（字符串）与 `conditions_structured`（结构化）**双写**，以结构化为主、字符串为运行时缓存。加载时若只有字符串则反编译；反编译失败则 `structured=false` 透传。确保任何旧剧本都能打开、不丢数据。

### 10.3 新增：规则卡片（L2 的存储）

```gdscript
# RuleCard：L2 层的逻辑单位，编译为事件/触发器
RuleCard := {
    "id": "rule_xxx",
    "name": "守卫发现小偷",
    "module": "ai_behavior",        # 所属玩法模块
    "trigger": StructuredCondition,  # 当（触发器）
    "conditions": [StructuredCondition],  # 且（条件）
    "actions": [StructuredAction],   # 就（动作）
    "enabled": true,
    "compiled_to": "event:ev_xxx",   # 编译产物指针（规则卡片最终落到事件系统运行）
}
```

规则卡片存储在 `event_system` 新增的 `rule_cards` 数组，保存时编译为引擎可执行的事件/触发条件。**L2 规则是"编辑器侧的高级抽象"，运行时看到的永远是标准事件系统**——这保证了游玩器零改动。

### 10.4 数据模型总览

```
WorldScriptData（单源）
├── metadata（script.json）
├── worldview        → L1表单直读
├── event_system     → L1事件卡片 / L2规则卡片(rule_cards) / L3蓝图(blueprint_graphs)
│   └── conditions: 字符串 ⇄ conditions_structured: 结构化（双向编译）
├── economy_system   → L1表单 / L2规则（市场联动）
├── ability_system   → L1表单 / L2规则（成长触发）
└── metadata.mud_data → MUD编辑器 / 保存时导出 mud/*.txt
```

---

## 11. 关键功能清单与优先级

### P0（地基，缺此一切免谈）
- [ ] 结构化条件/动作模型 + 双向编译器（`condition_compiler.gd`）
- [ ] L1 表单层：世界观/角色/物品表单 + 事件卡片（复用 `panels/`）
- [ ] 层级切换器（L1/L2/L3 视图切换，数据不变）
- [ ] 模板"成品化"：每个题材分支一个开箱即玩的示例剧本

### P1（核心体验，决定留存）
- [ ] L2 When-Do 规则卡片 + 条件/动作下拉选择器
- [ ] 世界速览面板（右侧实时反馈）
- [ ] 诊断产品化：人话报错 + 点击定位 + 一键修复建议
- [ ] F7 试玩起点选择（从当前事件开始）
- [ ] 新手向导（任务式清单）

### P2（深度与闭环）
- [ ] 时间旅行调试（状态快照 + 时间轴回放）
- [ ] 导演视角（事件触发浮标 + 条件实时面板）
- [ ] 事件链流程图可视化（复用 event_chains）
- [ ] 规则卡片 ⇄ 节点子图互转（L2→L3 升级通道）
- [ ] 模板 remix 画廊 + 教学注释

### P3（高上限与生态）
- [ ] 蒙特卡洛快跑（经济/流程平衡模拟）
- [ ] AI 试玩员（LLM Agent 走查，GDD §8.3）
- [ ] 玩法模块联动推荐（§5.5）
- [ ] 2D 场景编辑器接入逻辑触发器（Phase 3 工作区）
- [ ] 社区分享/评价（GDD Phase 3）

---

## 12. 分阶段实施路线图

与现有"万界诗篇UI全面升级"计划及 GDD 里程碑对齐：

| 阶段 | 周期 | 目标 | 交付物 | 验收标准（可量化） |
|------|------|------|--------|------------------|
| **A 地基** | 3-4 周 | 结构化条件 + L1 表单层 | `condition_compiler.gd`、L1 视图、层级切换器 | 小白 30 分钟内用表单做出一个含 3 事件的可玩剧本；旧剧本 100% 可打开不丢数据 |
| **B 规则** | 4 周 | L2 规则卡片 + 诊断 | When-Do 卡片、世界速览、人话报错 | 用户用规则卡片实现"会涨价的市场"；报错可一键定位 |
| **C 试玩** | 3 周 | 编辑-运行闭环 | 试玩起点、时间旅行、导演视角 | 编辑→试玩往返 <3 秒；可回放任意事件 |
| **D 进阶** | 4 周 | L2⇄L3 互通 + 模板生态 | 规则转节点图、remix 画廊、新手向导 | 规则卡片可无损转为节点图；模板可试玩可 remix |
| **E 智能** | 持续 | 模拟 + AI + 社区 | 蒙特卡洛、AI 试玩员、分享 | 模拟 100 玩家 <10 秒；AI 能发现人工埋的死路径 |

**里程碑原则**：每个阶段结束时，产品都必须是"一个小白能用来做出可玩游戏的完整工具"，而不是"半成品功能的堆叠"。A 阶段结束就应能跑通 §2.2 的黄金路径。

---

## 13. 风险与应对

| 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|
| 结构化条件反编译覆盖不全，旧数据丢失 | 中 | 高 | `raw` 透传兜底 + 双写 + 迁移前备份；反编译失败率纳入测试指标 |
| L1/L2/L3 投影不一致导致数据错乱 | 中 | 高 | 单一活动视图原则 + 切层强制提交 + 投影层单元测试全覆盖 |
| 三层开发量大，拖垮进度 | 高 | 中 | 严格按 P0→P3 分期；L1 最大化复用 `panels/`；L3 完全复用现有蓝图 |
| 小白仍觉得难（地板不够低） | 中 | 高 | 每阶段做真实小白测试（找没做过游戏的人试用），以"30分钟做出可玩作品"为硬指标 |
| 规则卡片表达力不足，进阶用户受挫 | 中 | 中 | L2→L3 一键升级通道保底；规则卡片表达不了时明确提示并引导升级 |
| 试玩性能差（重载慢） | 低 | 中 | 引擎已支持重建；剧本定义懒加载（GDD §6.5）；快照增量化 |

---

## 14. 附录：竞品深度借鉴清单

| 竞品 | 具体借鉴点 | 落地到本方案 |
|------|-----------|-------------|
| **Scratch** | 低地板+高天花板+宽墙壁；积木只能合法拼合 | §1.3 理念；§6.1 预防式防错 |
| **GDevelop/Construct** | When-Do 事件表；条件/动作下拉化 | §5.2 规则卡片（L2 核心） |
| **Twine** | 卡片式叙事；链接即分支 | §4.2 L1 事件卡片；事件链可视化 |
| **Roblox Studio** | 模板库；remix 文化；6步新手引导 | §6.2 模板即游戏；§2.2 黄金路径 |
| **UE Blueprint** | 引脚类型着色+连线校验；搜索 | L3 保留（项目已实现 `validate_connection`） |
| **原神·千星奇域** | 零代码节点图的可行性 + 其失败教训 | §1.4 痛点清单 → 本方案的连线交互/搜索/调试设计 |
| **RPG Maker** | 表单化数据库；命令式事件 | §4.2 L1 表单；检查器 |
| **Minecraft** | 规则涌现；红石（简单原语组合出复杂系统） | §5.5 模块联动涌现 |

---

## 结语

本项目已经拥有了令人惊讶的"高天花板"——蓝图节点图、代码生成、四大引擎、MUD 数据管线、Godot 风格 IDE。这些是许多创作平台梦寐以求的专业底座。

**现在要做的不是再加高天花板，而是把地板降下来、在地板和天花板之间修好楼梯。**

三层渐进编辑模型（L1 表单 → L2 规则 → L3 图表）+ 结构化条件改造 + When-Do 规则卡片 + 模板即游戏 + 随时试玩闭环，就是这条楼梯。它不推翻任何现有成果，而是给现有成果加上一个"面向普通人"的入口和一条"自然成长"的路径。

当一个从没做过游戏的学生，能在 30 分钟内把自己脑海里的奇幻世界变成一个朋友可以游玩的游戏——"万界诗篇"就真正成为了它的名字：**每个人都是造物主，每个世界都是一首诗。**

